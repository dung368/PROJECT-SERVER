Back end of [child-mobile](https://github.com/dung368/child_mobile/).
## Installation
1. Replace firebase-config.json file with your own
2. Run ```pip install -r requirements.txt``` from root directory.
3. Run ```fastapi run server.py``` from root directory to start the server.
